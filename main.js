/**
 * Remove ads
 */
const ads = document.querySelector('#ads')
const footerAds = document.querySelector('.results--ads')

if (ads) {
  ads.style.display = 'none'
}

if (footerAds) {
  footerAds.style.display = 'none'
}